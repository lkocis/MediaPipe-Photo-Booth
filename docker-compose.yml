version: "3.9"
services:
  photobooth:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./photos:/app/photos
    devices:
      - /dev/video0:/dev/video0    # kamera
    restart: unless-stopped